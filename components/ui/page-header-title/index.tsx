import React from "react";
import { styled } from "../../../stitches.config";
import Typography from "../typography";

const PageHeaderTitle = styled("h5", {
  fontSize: "$md",
  fontWeight: "500",
  color: "$text500",
});
export default PageHeaderTitle;
