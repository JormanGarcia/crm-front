import { styled } from "stitches.config";

export const Fieldset = styled("fieldset", {
    width: "100%",
    border: "none",
    display: "flex",
    flexDirection: "column",
    gap: 12,
    padding: 0,
    margin: 0,
});